# ===========================================
# App Environment Variables
# ===========================================
# These are embedded in the build (client-side)
# Must use VITE_ prefix to be exposed to code
# Access via: import.meta.env.VITE_*

# Example: API endpoint
# VITE_API_URL=https://api.example.com

# ===========================================
# Bot Setup Variables
# ===========================================
# NOT embedded in build (security)
# Used by setup script: ./scripts/setup-bot.sh

# Your bot token from @BotFather
BOT_TOKEN=

# Your deployed app URL (after first deploy)
APP_URL=
